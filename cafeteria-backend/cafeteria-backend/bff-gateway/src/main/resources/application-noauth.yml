# Perfil de desarrollo SIN autenticacion. Activar con: SPRING_PROFILES_ACTIVE=noauth
app:
  security:
    enabled: false

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.resource.reactive.ReactiveOAuth2ResourceServerAutoConfiguration
